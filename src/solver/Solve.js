class Solve{


    constructor(board){

        this.board = board;
        this.boardMap = new Map(); // store boards to avoid duplicates


    }

    // assume board is 2D array that looks like [[x,x,x,x,x]
    //                                          [x,x,x,x,x]
    //                                          [x,x,x,x,x]]
    // assume you have board.swap(some_target_position) that returns a new board
    // assume you have board.calcCombos() that returns the number of existing combos
    //
    // eventually you want to return sequence of moves like [(0,0),(0,1),(0,2)...] where each element is (row,column)
    search(){






    }






}